#include <stdio.h>
#include <stdlib.h>

int main() {
    int x = 10;
    int y = 20;
    int z = 30;
    
    if (x < y) {
        printf("%d\n", x + y);
    } else {
        printf("%d\n", x - y);
    }
    
    return 0;
}
